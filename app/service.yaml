apiVersion: v1
kind: Service
metadata:
  name: mytomcat8webapp
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    run: mytomcat8webapp
  type: NodePort
